{
  "name": "anaxa-v1",
  "version": "1.0.0",
  "description": "ANAXAGORAS — V1 | WhatsApp Bot (Termux Ready)",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "termux": "node index.js"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.9",
    "pino": "^8.19.0",
    "qrcode-terminal": "^0.12.0"
  },
  "notes": "No native/compiled dependencies are used (no sharp, no canvas, no better-sqlite3) so this runs on Termux without build-essential / clang."
}
